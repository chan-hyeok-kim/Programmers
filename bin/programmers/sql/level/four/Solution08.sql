SELECT FLAVOR
  FROM (
SELECT FG.FLAVOR,SUM(FG.TOTAL_ORDER), RANK() OVER(ORDER BY SUM(FG.TOTAL_ORDER) DESC) AS RANK
  FROM FIRST_HALF FG INNER JOIN JULY J
    ON FG.FLAVOR=J.FLAVOR
 GROUP BY FG.FLAVOR
)   
 WHERE RANK<4;
-- 주문량이 많은 아이스크림들 조회하기